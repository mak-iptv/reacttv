{
  "version": 2,
  "buildCommand": "CI=false DISABLE_ESLINT_PLUGIN=true npx react-scripts build",
  "installCommand": "npm install --legacy-peer-deps",
  "outputDirectory": "build",
  "framework": "create-react-app",
  "regions": ["fra1"],
  "env": {
    "CI": "false",
    "DISABLE_ESLINT_PLUGIN": "true"
  }
}
